Implemented a simple web crawler called wgetX.
When use it:
Enter "make" at the command line to compile.
Enter "./wgetX <url>" or "./wgetX <url> <path>" with the website URL and the file storage path.